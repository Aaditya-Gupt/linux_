# linux_
use PWD command to see the path
LS to see whats around you
absolute path start at base(/) directory 
relative paths start from current directory
every directory has . and .. as hidden folders
